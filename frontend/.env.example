# Frontend Environment Variables Example
# Copy this file to .env for local development
# In production (Render), set these in the Environment tab

# API Backend URL
# Development: Use relative path /api (Vite proxy)
# Production: Use your Render backend URL
VITE_API_URL=https://your-backend-name.onrender.com

# WebSocket URL (if different from API URL)
# Usually the same as VITE_API_URL
VITE_WS_URL=https://your-backend-name.onrender.com
